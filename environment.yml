name: sbk-analytics
channels:
  - conda-forge
  - pytorch
dependencies:
  - python=3.10
  - pip
  - pyyaml
  - requests
  - pytorch
  # Manual development host only. sbk-analytics never installs sbk-charts into
  # this Conda environment; charts uses a selected ready-to-run local command
  # or its own versioned venv based on sbk-config.env.
