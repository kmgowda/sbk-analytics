name: tests

on:
  push:
  pull_request:

jobs:
  unit:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        python: ["3.9", "3.12"]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python }}
          cache: pip
      - run: python -m pip install --upgrade pip
      - run: python -m pip install -e .
      - run: python -m unittest discover -s tests -v

  windows-portability:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
      - run: python -m pip install --upgrade pip
      - run: python -m pip install -e .
      - name: Windows cache, config, archive, and CLI tests
        run: |
          python -m unittest discover -s tests -p "test_properties.py" -v
          python -m unittest discover -s tests -p "test_dependency_hardening.py" -v
          python -m unittest discover -s tests -p "test_process_cleanup.py" -v

  conda:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash -el {0}
    steps:
      - uses: actions/checkout@v5
      - uses: conda-incubator/setup-miniconda@v4
        with:
          activate-environment: sbk-analytics
          environment-file: environment.yml
          auto-activate-base: false
      - run: python -m pip install -e .
      - run: python -m unittest discover -s tests -v
